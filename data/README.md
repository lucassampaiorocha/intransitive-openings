# Runtime data

Place `openings_community.sqlite3` in this directory for local use. Database files,
WAL files and shared-memory files are excluded from version control.

Before the first Turso upload, stop writers and run `python opening_prepare.py` from
the application directory.
