// Leave empty for both opening_server.py and the combined Vercel deployment.
// Set only when the static UI and API intentionally use different origins.
window.OPENINGS_API_BASE = '';
// The opening explorer owns its assets. Leave empty unless a separate asset CDN is used.
window.OPENINGS_ASSET_BASE = '';
